{
  "name": "watch-the-drying",
  "version": "0.1.0",
  "private": true,
  "description": "Watch the Drying -- paint a small section of wall, then watch the paint dry. The drying clock only runs while the tab is actually visible and focused.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=0.12.0",
    "iojs": ">=1.5.1"
  }
}
